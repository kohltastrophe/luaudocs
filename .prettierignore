# Fetched, built, or otherwise never hand-edited.
node_modules/
dist/
src/generated/
bun.lock

# Byte-exact extractor output. docmodel-*.json is what `regen:docmodels --check`
# compares against, and the fixture projects are deliberately odd input whose
# line numbers the captures encode: reformatting either breaks CI.
test/fixtures/
test/__snapshots__/

# Tool-owned output of this repo's own site, regenerated by every build.
.luaudocs/.vitepress/
.luaudocs/api/
.luaudocs/package.json
.luaudocs/public/

# The guides' tables are hand-aligned, and markdown is reviewed by reading it,
# not by a printer width.
*.md
